<%= render "shared/header" %>
    <%= yield %>
    <div class= mein-back>
    <div class="bg-mask">
    <div>
    <h1>MIRAIでは予めに起こるであろう事を日記スタイルにし実際にその日とのブレを比較するスタイルの日記です。</h1>
    <% if user_signed_in? %>
    <%= link_to 'フォロー一覧', followings_path(current_user.id)%>
    <%= link_to 'フォロワー一覧', followers_path(current_user.id)%>
    <% end %>
    <p id="notice"><%= notice %></p>

<h1>未来の日記</h1>

<table>
  <thead>
    <tr>
      <th>タイトル</th>
      <th>時間</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @diaries.each do |diary| %>
      <tr>
        <td><%= diary.title %></td>
        <td><%= diary.start_time.strftime("%Y-%m-%d %H:%M") %></td>
        <td><%= link_to '日記詳細', diary %></td>
        <td><%= link_to '編集', edit_diary_path(diary.id) %></td>
        <td><%= link_to '日記削除',diary_path(diary.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


<%= month_calendar events: @diaries do |date, diaries| %>
  <%= date.day %>

  <% diaries.each do |diary| %>
    <div>
      <%= link_to diary.title, diary %>
    </div>
  <% end %>
<% end %>
  </div>
  </div>
  </div>

    <footer>
      <%# <p>
        Copyright PicTweet 2019.
      </p> %>
    </footer>
  </body>
</html>
