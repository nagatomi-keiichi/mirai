<%= render "shared/header" %>

<%# <p>
  <%= link_to "by #{diary.user.name}", user_path(diary.user.id), class: :card__user %>
<%# </p> %>
<p>
  <strong>投稿者</strong>
  <%= @diary.user.name %>
</p>
 
<p>
  <strong>タイトル</strong>
  <%= @diary.title %>
</p>

<p>
  <strong>投稿日時</strong>
  <%= @diary.start_time.strftime("%Y-%m-%d %H:%M") %>
</p>

<p>
  <strong>出来事</strong>
  <%= @diary.content %>
</p>

<%= image_tag @diary.image  %>

<%= link_to '編集', edit_diary_path(@diary) %> 
<%= link_to '削除', diary_path(@diary.id), method: :delete %>